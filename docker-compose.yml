# PRDForge Docker Compose Configuration
# This is a placeholder - will be implemented in Phase 2

version: '3.8'

services:
  # Backend API server
  # api:
  #   build: .
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./data:/app/data
  #   environment:
  #     - DATABASE_URL=sqlite:///data/prdforge.db

  # Frontend dashboard
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - api

volumes:
  data:
